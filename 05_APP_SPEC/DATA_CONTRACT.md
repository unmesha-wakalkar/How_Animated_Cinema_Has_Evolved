# Data Contract

The app should treat `02_DATA/JSON/films_master.json` as the canonical record collection.

Derived aggregates come from `story_metrics.json`.
Filter options come from `facets.json`.
Milestones come from `historical_timeline.json`.
Media references come from `source_manifest.json` and `video_sources.json`.
